name: AWS OIDC demo

on:
  push:
    branches:
      - main

permissions:
  id-token: write       # <-- required for OIDC
  contents: read

jobs:
  aws-test:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.ACCOUNT_ID }}:role/${{ secrets.OIDC_GITHUB_ROLE }}
          aws-region: ap-south-1  # or your preferred region

      - name: Verify caller identity
        run: aws sts get-caller-identity

      # Example: list S3 buckets (if allowed)
      - name: List S3 buckets from account 631478159968
        run: aws s3 ls
